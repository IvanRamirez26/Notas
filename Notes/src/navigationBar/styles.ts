import styled from "styled-components";

const Styled = {
  NavBar: styled.div`
    display: flex;
    justify-content: space-between;
    padding-left: 50px;
    padding-top: 10px;
    padding-right: 50px;
  `,
};
export default Styled;
